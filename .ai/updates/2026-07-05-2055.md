# Session Update — 2026-07-05-2055

## Created
- `src/app/coming-soon/page.tsx` — New coming soon page at `/coming-soon` using GSAP for all animations

## Animations (GSAP)
- **Entrance timeline**: badge → title → tagline → countdown cards (staggered) → CTA, all with `power3.out` easing
- **Glow ring**: continuous 360° rotation (25s loop)
- **Seconds card**: `scale` + `borderColor` pulse on every tick

## Key details
- Uses design system `Button` component (accent + outline variants, lg size)
- Features: countdown timer (Hari/Jam/Menit/Detik), gradient title, pulsing seconds card with supernova-orange accent, rotating conic-gradient glow ring, nebula blobs
- Background: `bg-gradient-to-br from-deep-space via-midnight-navy to-deep-space` (dark cosmic gradient, complements the StarBackground in root layout)
- No `asChild` usage to avoid Radix Slot SSR prerendering issue (uses `router.push` instead)
