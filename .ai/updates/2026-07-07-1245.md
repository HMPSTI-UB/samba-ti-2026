# Session Update — 2026-07-07-1245

## Task
Mengerjakan 2 task Diego dengan deadline 8 Juli 2026:
1. Create Credentials Login View
2. Create Auth API Call

## File yang Dibuat

### Login View
- `src/app/auth/login/layout.tsx` — Auth layout: fullscreen centered, nebula background orbs, tanpa navbar/footer
- `src/app/auth/login/page.tsx` — Route page tipis, delegate ke `LoginForm`
- `src/features/auth/components/login-form.tsx` — LoginForm `'use client'`:
  - Fields: `username` + `password`
  - Password show/hide toggle (Lucide Eye/EyeOff)
  - Framer Motion stagger entrance animation (fadeUp dengan custom delay)
  - Field-level error display dari Server Action
  - Loading state + disabled saat pending
  - Design system: `glass-panel`, `Input`, `Button` components
  - Orbitron font untuk heading, Inter untuk body

### Auth API Call
- `src/features/auth/actions/login-action.ts` — Server Action `'use server'`:
  - Memanggil `serverApi.post('/auth/login', { username, password })`
  - Set `access_token` (15 menit) + `refresh_token` (7 hari) sebagai http-only cookies
  - Return `{ success, user }` atau `{ success: false, message, errors }`
  - Handles `ApiError` untuk field-level validation errors
- `src/features/auth/hooks/use-login.ts` — Custom hook `useLogin`:
  - Wraps `useMutation` + `loginAction`
  - Field error state management
  - Sonner toast untuk success/error feedback
  - Redirect ke `/portal` setelah login berhasil

### Update
- `src/lib/api/auth.ts` — `LoginInput` type diupdate dari `email` → `username` (sesuai Credentials Login panitia)

## Catatan
- Token tidak pernah menyentuh client JavaScript (http-only cookies via Server Action)
- 401 auto-refresh sudah di-handle oleh Axios interceptor di `src/lib/api/client.ts`
- Redirect ke `/portal` setelah login — route `/portal` belum dibuat (next task: Dashboard Layout)
- Backend endpoint yang dibutuhkan: `POST /auth/login` dengan body `{ username, password }`
